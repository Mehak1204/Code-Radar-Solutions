#include<stdio.h>
int main()
{
    char name[50];
    int age;
    char hobby[50];
    fgets(name, size of(name), stdin);
    scanf("%d", &age);
    fgets(hobby, sizeof(hobby), stdin);
    printf("Name: %s", name);
    printf("Age: %d\n", age);
    printf("Hobby: %s", hobby);
    return 0;
}